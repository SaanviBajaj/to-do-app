apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - all.yaml

commonLabels:
  app.kubernetes.io/part-of: to-do-app
  app.kubernetes.io/component: frontend

images:
  - name: image-registry.openshift-image-registry.svc:5000/gitops-run/todo-web
    newName: image-registry.openshift-image-registry.svc:5000/gitops-run/todo-web
    newTag: latest
